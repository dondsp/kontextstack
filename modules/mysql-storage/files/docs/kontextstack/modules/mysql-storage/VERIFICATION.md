# Verification

Run the no-connection manifest check, migrate a disposable local database twice, reject changed/failed history, and prove persistence across pool restart. Rehearse restore before production.

Run kit/checks after adapting references into the project. Inspect actual emitted cookies, protected APIs and logs; the unit service tests alone do not establish hosted/browser acceptance. Record installed, implemented, locally verified, provider configured, deployed, migrated, production verified and owner accepted separately.
