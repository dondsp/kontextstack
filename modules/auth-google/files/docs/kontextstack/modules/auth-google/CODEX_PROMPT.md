# Copy into Codex in the selected project

Read all applicable AGENTS.md and architecture instructions first. Inspect the actual repository and read this module's decision.json, evidence.json, implementation.json and ROLLBACK.md. Separate confirmed facts, assumptions and missing inputs.

Adapt google-auth.mjs server-side with jose 6.2.12 pinned in the project lockfile. Its default verifier uses only Google HTTPS JWKS and RS256. The injection points are for trusted server tests, never browser input. Configure exact origin/callback and environment-only client values. Start via an origin-checked POST, place the returned binding only in its HttpOnly cookie, and redirect to the returned fixed Google URL. The callback reads that cookie and existing session cookie server-side, uses the exact request URL, and clears the OAuth cookie on every outcome. Store pending state in the shared durable adapter; consume it atomically before exchange. Never log query strings, authorization codes or JWTs. Identity-only scopes are fixed; no offline access. Email never auto-links accounts. Linking requires a separate user action with CSRF and a recent local session; user roles stay local. No provider tokens are retained.

Modify only the selected project, adapt templates without replacing working architecture, and never reveal or commit secret values. Run these checks: Use signed synthetic ID tokens to test signature/issuer/audience/azp/expiry/nonce/email/hd, exact callback, single-use state and browser binding, concurrent replay, account collisions, explicit linking, disconnect and provider disable during exchange.

Pause before external, production, destructive or data-changing operations and show exact target/effect/verification/recovery for owner approval. Installing this kit grants none of that authority. Keep implementation, verification, deployment, migration and acceptance distinct. Update project-owned production/recovery documents with non-secret evidence. Report manual/provider work still outstanding precisely.
