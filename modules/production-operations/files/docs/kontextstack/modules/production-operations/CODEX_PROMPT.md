# Continue in the selected project

Read applicable AGENTS.md and architecture documents first. Inspect the actual project, decision.json, evidence.json, implementation.json and ROLLBACK.md. Separate confirmed evidence from assumptions and gaps.

Adapt readiness.json, monitoring.json, incident.md, backup-restore.md, rotation-maintenance.md and the recovery decision tree into the project-owned operations records. Select only real surfaces in selection.json; a static site must not inherit database/auth gates. Run the inert readiness evaluator explicitly after adapting it: it requires same-revision passed evidence for every selected surface and always returns ownerAccepted false and externalAuthority false. Evidence supplied by the owner is reported evidence, not independent proof by the toolkit. Backup existence and restore rehearsal are separate gates. Keep exact artifact, runtime, DNS, schema and identity recovery references. Assign owners and due dates to monitoring, retention/deletion, capacity, certificates, dependencies and recovery exercises. Record a representative journey in addition to technical health. Use synthetic data in local rehearsals; never import production backups into the repository.

Modify only this selected repository. Adapt templates without replacing working architecture or overwriting customized files. Never reveal or commit secrets. Run these checks: Evaluate static-only and mature runtime/data/auth fixture records. Missing surfaces, stale revision evidence and absent restore rehearsal must block readiness. Completing all local record fields makes the release eligible for owner review but never accepted automatically. Simulate partial file delivery, generate the exact prior-artifact recovery differences and rehearse recovery in a synthetic target. Verify that file recovery does not claim database or identity recovery. Test stopping before provider approval and resuming with that state still unapproved. Review mobile/browser/keyboard journeys on the actual product when separately authorized.

Pause before every external, production, destructive or data-changing action. Show exact scope and recovery and obtain owner approval. Keep implementation, local verification, deployment, restart, migration and acceptance separate. Update project-owned production and recovery documents with non-secret references and report remaining provider actions precisely.
