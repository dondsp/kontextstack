# Owner selection

Create .kontextstack/modules/production-operations/selection.json before preview, using only these enumerated choices:

```json
{
  "surfaces": [
    "static"
  ]
}
```

This selects a local kit. It does not approve any production action.
