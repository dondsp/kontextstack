# Owner selection

Create .kontextstack/modules/github-cpanel-deploy/selection.json before preview, using only these enumerated choices:

```json
{
  "target": "static"
}
```

This selects a local kit. It does not approve any production action.
