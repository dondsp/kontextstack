# Prepare approval-gated cPanel delivery

Inspect first. This local kit grants no provider or production authority. Before preview create .kontextstack/modules/github-cpanel-deploy/selection.json with the exact non-secret choices shown by modules inspect:

```json
{
  "target": "static"
}
```

The module reads and fingerprints this owner-maintained record but never overwrites it. Missing evidence blocks preview. Resolve the project Git state, then inspect → preview → exact approval → apply → verify. Read IMPLEMENTATION.md and CODEX_PROMPT.md to adapt the installed references.

Source: https://github.com/dondsp/kontextstack
