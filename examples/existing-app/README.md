# Existing-app example

This project shape represents a mature repository that may already have a
runtime, database, authentication, deployment, and operational decisions. The
v0.1 handoff core records current context without adding or replacing those
capabilities. Later modules must be selected only after inspection and preview.
